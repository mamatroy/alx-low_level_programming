#include "main.h"
/**
 * _islower-Checks if a letter is uppercase
 * @x:The number to be checked
 * Return:1 if uppercase or otherwise return 0
 */
int _islower(int x)
{
	if (x >= 65 && x <= 98)
	{
	return (1);
	}
	return (0);
}
