#include "main.h"
/**
 * _isupper-Check if a letter is uppercase
 * @x:The number to be checked
 * Return:1 if uppercase or otherwise return 0
 */
int _isupper(int x)
{
	if (x >= 65 && x <= 98)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
